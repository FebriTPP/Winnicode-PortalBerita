## Installation

1. Copy `.env.example` ubah namanya ke `.env`

2. Buka Terminal dan ikuti command dibawah:

- `composer update`
- `php artisan key:generate`
- `php artisan migrate:fresh --seed`
- `php artisan serve`
